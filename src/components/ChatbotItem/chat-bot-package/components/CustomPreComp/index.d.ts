import { ReactNode } from "react";
interface CustomPreCompProps {
    children: ReactNode;
}
declare const CustomPreComp: ({ children }: CustomPreCompProps) => import("react/jsx-runtime").JSX.Element;
export default CustomPreComp;
